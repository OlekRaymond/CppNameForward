# CppNameForward
Name forwarding in C++ can be very boilerplate heavy requiring a definition for each method you want to wrap. This library aims to provide a much easier method to name forward items to the standard library
